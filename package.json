{
  "name": "instagetter",
  "version": "1.0.0",
  "private": true,
  "description": "Service behind insta.skylar.technology — public status page plus a token-protected API.",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsx --env-file=.env src/server.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.0.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
